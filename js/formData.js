let fetchedData = null;
let contactForm = document.querySelector('#croxoContactForm')
!!(typeof customFormData !== 'undefined') 
    && (fetchedData = customFormData); 

console.log('all done', fetchedData, contactForm)

if(contactForm) {
    debugger
}

/*
!!contactForm && (
  
)
*/
